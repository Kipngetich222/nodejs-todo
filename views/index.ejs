<html>
<head> 
    <title> ToDo App </title>
    <link href="https://fonts.googleapis.com/css?family=Lato:100" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <h2> ToDo App </h2>

        <form action="/addtask" method="POST">
            <input type="text" name="newtask" placeholder="Add new task">
            <button> Add Task </button>
        </form>

        <h2> Saved Task </h2>
        <form action="/removetask" method="POST">
            <% for (var i = 0; i < task.length; i++) { %>
                <li>
                    <input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %>
                </li>
            <% } %>
            <button type="submit" id="top"> Clear Completed </button>
        </form>

        <h2> Completed </h2>
        <form action="/deletecomplete" method="POST">
            <% for (var i = 0; i < complete.length; i++) { %>
                <li>
                    <input type="checkbox" name="deleteCheck" value="<%= i %>" /> <%= complete[i] %>
                </li>
            <% } %>
            <button type="submit"> Delete Selected </button>
        </form>
    </div>
</body>
</html>
